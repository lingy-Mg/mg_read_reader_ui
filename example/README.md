# novel_reader_ui example

Android/Windows 示例宿主，演示如何向阅读器注入异步书籍数据、进度、偏好设置、书签存储和退出事件。

```bash
flutter run -d <android-device>
```

在 Windows 主机上：

```powershell
flutter run -d windows
```

示例正文《山灯未眠》为项目原创演示内容。
